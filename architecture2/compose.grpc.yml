cd ~/Desktop/DS_project_2/architecture2

# Overwrite compose.grpc.yml with a clean, valid file
cat > compose.grpc.yml <<'YAML'
services:
  timescaledb:
    image: timescale/timescaledb:latest-pg16
    environment:
      - POSTGRES_PASSWORD=postgres
    ports:
      - "55432:5432"        # host:container (changed host port from 5432 -> 55432)
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6381:6379"         # host:container (changed host port from 6379 -> 6381)
    restart: unless-stopped

  ingestion:
    build:
      context: .
      dockerfile: services/ingestion/Dockerfile
    environment:
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis
    ports:
      - "50051:50051"
    restart: unless-stopped

  aggregator:
    build:
      context: .
    environment:
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis
    ports:
      - "50053:50053"
    restart: unless-stopped

  alerter:
    build:
      context: .
    environment:
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis
    ports:
      - "50054:50054"
    restart: unless-stopped

  queryapi:
    build:
      context: .
      dockerfile: services/query-api/Dockerfile
    depends_on:
      - timescaledb
    ports:
      - "50052:50052"
    restart: unless-stopped
YAML

